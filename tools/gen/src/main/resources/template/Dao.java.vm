package ${package}.dao;

import ${package}.model.${project}.${className};
import ${package}.dao.base.BaseDao;
import ${req}.${className}ReqVO;
import ${resp}.${className}RespVO;

/**
 * ${comments}
 * 
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
public interface ${className}Mapper extends BaseDao<${className},${className}ReqVO,${className}RespVO> {
}

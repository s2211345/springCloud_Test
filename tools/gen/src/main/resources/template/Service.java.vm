package ${package}.service;

import ${package}.model.${project}.${className};
import ${package}.service.base.BaseService;
import ${req}.${className}ReqVO;
import ${resp}.${className}RespVO;

/**
 * ${comments}
 * 
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
public interface ${className}Service extends BaseService<${className},${className}ReqVO,${className}RespVO>{
}

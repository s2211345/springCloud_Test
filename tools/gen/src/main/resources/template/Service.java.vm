package ${package}.service;

import ${package}.model.${className};
import ${package}.dto.PageDTO;
import ${package}.service.base.IService;
import ${package}.vo.req.${className}ReqVO;
import ${package}.vo.resp.${className}RespVO;

import java.util.List;
import java.util.Map;

/**
 * ${comments}
 * 
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
public interface ${className}Service extends IService<${className}>{
    PageDTO<List<${className}RespVO>> getList(${className}ReqVO reqVO);

    int getCount(${className}ReqVO reqVO);

    int delete(Long id);
}
